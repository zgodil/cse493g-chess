# Experiment 1: ViT-B/16 + flat MLP head, pretrained, no auxiliary heads
# Tests whether ViT features alone improve over a CNN without spatial exploitation.

data:
  dataroot: "dataset"
  img_size: 224
  num_workers: 4
  pin_memory: true

model:
  head: flat
  aux_heads: []
  pretrained: true
  num_piece_classes: 13
  num_orient_classes: 4

loss:
  w_corners: 0.0
  w_occupancy: 0.0
  w_orient: 0.0
  label_smoothing: 0.1

training:
  epochs: 50
  batch_size: 8
  optimizer:
    lr: 5.0e-5
    weight_decay: 1.0e-2
  scheduler:
    t_max: 50
    warmup_epochs: 5
    eta_min: 1.0e-6
  early_stopping:
    patience: 10
  checkpoint:
    save_dir: "checkpoints/exp1"
    monitor: "val_exact_acc"
    mode: "max"
  mixed_precision: false

inference:
  checkpoint: "checkpoints/exp1/best_model.pt"
